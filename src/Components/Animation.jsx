import './Animation.css'; // Import the CSS file where the animation is defined
import React from 'react';
const Animation = () => {
    return (
        <div className="animation w-100 ">
            {/* This div will use the animation defined in the CSS */}
            Animated Background
        </div>
    );
};

export default Animation;